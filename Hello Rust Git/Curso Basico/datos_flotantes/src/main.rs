fn main() {
    //Flotantes Float
    let mut _num1: f32=3.14159265; // _ es para aclarar a rust que yo puse a proposito una variable mutable, por que si 

    let mut _num2: f64=3.14159265;  // f64 por que ya no alcanza los bits para el pi en el f32, por falta de espacio o chamba.

    let mut _num3= 3.14159265;
    println!("Numero: {}", _num1);
    println!("Numero: {}", _num2);
    println!("Numero: {}", _num3);
}
